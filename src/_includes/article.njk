{% extends "./base.njk" %}

{% block title %}
{{ title }}
{% endblock %}

{% block main %}
<article>
    <h1>{{ title }}</h1>
    <aside>
        <p>
            公開: <time datetime="{{ date }}">{{ date }}</time>
        </p>
        <p>
            著者: {{ author }}
        </p>
        <div>
            tag: 
            {% for tag in tags %}
            <span>{{ tag if tag !== "posts" }}</span>
            {% endfor %}
        </div>
    </aside>
    <div>
        {{ content | safe }}
    </div>
</article>
{% endblock %}